# 0809
# terminalはコマンドと同義　最初にgit initした
# 左バナー、上から３番目（ソースこんとろーる）クリック
# 該当するファイルの＋ボタンを押す
# コミットメッセージ
# publish　→ リモートリポジトリにプッシュする
# 新規ファイルは左バナーの一番上

# staging　はcommit前の＋ボタンを押した状態のこと

プルリクテスト用

#0811
【自己解決したもの】
・コミットしたはずが、githubに反映されていない
 　⇨　git push をしないと反映されないっぽい。commitだけじゃだめっぽい
 　⇨　あるいは、ソースコントロールの3点リーダーからpushを選択する
・ブランチの切り替えってどこで確認できる？ブランチを切り替えて同じファイルを更新しても良い？
  ⇨　異なるブランチで同じファイルを見ると、最終コミットした段階の状態を閲覧することになる。
  　　⇨ master でpythonを見た時と sub　でpythonを見た時のファイルの状態が違う
・ブランチを切り替える前に変更をコミットしないといけない？

【質問】
VScodeで作った新しいbranchをgitbubに反映するにはどうしたら良いのか
⇨demoってのが作れない。github上は作れたのだけど(たぶんdemoとmasterが同じ扱い)git checkout demoができるけど、githubではmasterにマージされる

(HEAD -> master, tag: v1.0, origin/master)
これってどういう意味？

【めも】
git tag -a "tag name" -m "comment"
git show {tag name}
git push origin {tag name}
git tag -d {tag name}
⇨ {} は不要
